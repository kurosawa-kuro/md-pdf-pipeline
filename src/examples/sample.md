# Markdown PDF Pipeline Sample

このサンプルは、Markdown から PDF への最小変換を確認するための入力例です。

## テーブル

| 項目 | 内容 |
|---|---|
| 入力 | 単一 Markdown |
| 出力 | 単一 PDF |
| 本命 | Python + WeasyPrint |

## コード

```python
def hello(name: str) -> str:
    return f"hello, {name}"
```

## 引用

> Markdown を原本にし、PDF は閲覧用として扱う。
